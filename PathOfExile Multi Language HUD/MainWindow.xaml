<Window x:Class="PathOfExile_Multi_Language_HUD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Loaded="Window_Loaded" Unloaded="Window_Unloaded" Topmost="True"
        Title="MainWindow" Height="450" Width="800" WindowStyle="None" Background="Transparent" AllowsTransparency="True" ResizeMode="NoResize">
    <Grid>
        <Menu VerticalAlignment="Top" HorizontalAlignment="Left">
            <MenuItem Header="≡">
                <MenuItem Header="設定(Setting)" Click="MenuItem_Setting_Click"/>
                <MenuItem Header="關於(About)" Click="MenuItem_About_Click"/>
                <MenuItem Header="結束(Exit)" Click="MenuItem_Close_Click"/>
            </MenuItem>
        </Menu>
        <TextBlock Name="tooltip" FontSize="{Binding TextSize}" Foreground="{Binding TextColor}" Visibility="{Binding tooltip_Visibility}" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#E5DADADA"/>
    </Grid>
</Window>
